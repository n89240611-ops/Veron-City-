# VYRON City environment configuration
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db

# Admin bootstrap account (created by the idempotent content seed)
ADMIN_EMAIL=admin@vyron.city
ADMIN_PASSWORD=VyronAdmin!2026

# Set to true when serving over HTTPS so session cookies are Secure
COOKIE_SECURE=false
